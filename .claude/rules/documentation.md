# Documentation

- Update `README.md` when a code change affects setup, install, usage, or user-visible commands.
- Keep in-code comments in sync with the code they describe; delete stale comments rather than let them drift.
- Public API surface deserves a doc comment in the idiom of the language (godoc line, docstring, JSDoc). Match what the linter enforces.
